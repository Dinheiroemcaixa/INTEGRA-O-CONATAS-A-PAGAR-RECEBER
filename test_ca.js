const { createClient } = require('@supabase/supabase-js'); async function run() { const supabase = createClient(process.env.NEXT_PUBLIC_SUPABASE_URL, process.env.SUPABASE_SERVICE_ROLE_KEY); const { data: emp } = await supabase.from('empresas').select('access_token_conta_azul').not('access_token_conta_azul', 'is', null).limit(1); const token = emp?.[0]?.access_token_conta_azul; if (!token) return console.log('No token'); const cpf = '14417724000185'; const urls = ['https://api-v2.contaazul.com/v1/pessoas?cpf_cnpj=' + cpf, 'https://api-v2.contaazul.com/v1/pessoas?documento=' + cpf, 'https://api-v2.contaazul.com/v1/pessoas?search=' + cpf, 'https://api-v2.contaazul.com/v1/pessoas?busca=' + cpf, 'https://api-v2.contaazul.com/v1/pessoas?termo_busca=' + cpf, 'https://api-v2.contaazul.com/v1/pessoas?busca_textual=' + cpf]; for (const url of urls) { console.log('Testing:', url); const res = await fetch(url, { headers: { 'Authorization': 'Bearer ' + token } }); const data = await res.json(); console.log('Status:', res.status); const items = data.itens || data.items || data.content || data.data || []; console.log('Found:', items.length); } } run();
